server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/csu_db?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: root
    password: "dhwldbs15@@"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # 개발 단계: update / 실제 운영: validate 또는 none 권장
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

logging:
  level:
    org.hibernate.SQL: debug

# JWT 설정 (추가)
csu:
  jwt:
    secret: "vqpS97QOCXqDmGd5vDio9v2bA5ickxUn"
    expiration: 3600000   # 1시간 (밀리초 기준, 1000*60*60)
